# Ideas Session — 2026-04-25

## Context

User (Xi-Wei Pan, first author of the ProblemReductionPaper) proposes using
`problem-reductions` as the substrate for a new benchmark that measures
AI-agent bug-finding ability. Target venue: NeurIPS Datasets & Benchmarks
track. Rough notes from user:

- "Definition of bug for AI Agent"
- "24 hours find how many bugs"
- "Skills/Agents find counter example"
- "Benchmark/Arena"
- "Extension to benchmark on computation complexity"

## Phase 1 — Exchange 1 (mentor)

**Mentor's take.** The core asset is a machine-checkable oracle:
round-trip preservation + declared polynomial overhead. Unlike SWE-bench and
most code-bug benchmarks, grading can be fully deterministic and needs no
human/LLM judge. This is the pitch-worthy angle.

Three risks raised:

1. **Contamination.** Repo + paper are public. Without held-out or
   injected bugs, scores will be memorization.
2. **Bug supply.** Paper itself claims the library is well-verified;
   real bug density may be too low for a benchmark with discrimination.
3. **Arena vs Benchmark.** NeurIPS D&B prefers reproducible leaderboards
   over open "how-many-in-24h" contests. The two flavors can coexist
   (arena as demo, benchmark as main score) but the main axis needs picking.

Extension to "computation complexity" flagged as out-of-scope for paper 1
(different skill tree: agent-as-theorem-prover).

**Mentor's question back to user:** where does the bug corpus come from?
- (a) historical bugs mined from git log (retrospective, SWE-bench-like)
- (b) injected / mutation-style bugs (synthetic, controllable)
- (c) unknown bugs in a fixed live snapshot
- (d) other / combination

## Phase 1 — Exchange 2 (user)

User picks **(c) with a twist**: fix a benchmark version, and over time
as the library evolves (old bugs fixed, new problems/rules added), the
bug set also evolves — a *living benchmark* model.

## Phase 1 — Exchange 3 (mentor)

Conceded that (a)-historical approach has a counting-granularity problem
(1 fix commit = 1 bug? 3 rules touched = 3 bugs?). Pivoted toward a
mutation-based counterexample-search framing:
- 1 mutant = 1 test item by construction (no counting ambiguity)
- task framed as "find a counterexample" not "find a bug" (oracle is
  deterministic: round-trip pass/fail)
- 3-tier design proposed: Main score (mutation recall) + Taxonomy
  contribution + Living frontier.
- Also offered the "co-verification flywheel" framing.

## Phase 1 — Exchange 4 (user)

User correctly notes the library itself is not provably clean, even with
their harness. I reframed this as a feature, not a bug: round-trip oracle
is one-sided, so "clean baseline" is never needed. Main score stays on
mutants (known positives); bonus score captures counterexamples found in
supposedly-clean rules (genuine wild bugs).

## Phase 1 — Exchange 5 (user — sharp push)

User's key pushback: **"改一处连 unit test 也过不了吧"** — a naive mutant
breaks the test suite, so finding it is trivial. User defines bug as:
**"all tests pass, but latent failure input still exists"**.

This is the test-escaping / stubborn-mutant concept from Jia & Harman 2011
mutation testing survey. Reframed benchmark around a 2-stage filter:
generate mutants → drop those caught by test suite → confirm remainder
are truly buggy (not equivalent) via held-out round-trip check. What
remains = benchmark test set of "test-escaping bugs".

Three secondary wins noted:
- Self-consistent with Paper 1 narrative (harness is good, yet these bugs
  exist — benchmark measures what's left).
- Taxonomy of test-escaping bug patterns in reduction code is itself a
  contribution.
- Flywheel: found counterexamples become new unit tests, pushing the
  bug-discovery frontier harder each cycle.

Open question flagged: Stage-1 generation strategy — LLM vs handcrafted
operator vs higher-order mutation.

## Phase 1 — Exchange 6 (user) and Mentor response

User asks: **"单纯就比找到 bug 的数量不行吗"**. Mentor concedes
over-engineering, repositions the proposal as **Arena**, not Benchmark
(matching user's original "Benchmark/Arena" note). Recommends `by-rule`
dedup for cleanest leaderboard semantics. Distinguishes Benchmark
tradition (MMLU/SWE-bench, fixed denominator) from Arena tradition
(Chatbot Arena/Pwn2Own/MLE-bench, count-based, NeurIPS-acceptable).

## Phase 1 — Exchange 7 (user) — pilot planning

User picks `by-rule` dedup; admits not knowing latent-bug density;
requests pilot plan. Mentor delivers full pilot plan:
- Freeze snapshot (commit SHA)
- Build oracle as `problemreductions-cli verify` subcommand
- Sample ~20 rules across (suspicious / transitive / random)
- 3 agents × 20 rules × 4h compressed budget
- Decision gate at N rules-with-counterexample: ≥8 green, 3-7 yellow,
  ≤2 red (pivot to mutation-injected design)

Quick scan of submodule confirmed: `src/rules/*.rs` (per-rule files),
`src/unit_tests/rules/`, existing CLI to extend. Naming question raised
("pr-oracle" was a placeholder; fold into existing CLI).

## Phase 1 — Exchange 8 (user) — capability framing

User asks: **what AI agent capability does this benchmark reflect — we
need motivation**. Mentor crystallizes the motivation as
**"Test-Escaping Bug Discovery"** (primary), with two alternative
framings as supporting layers:

- (A) **Test-Escaping Bug Discovery** — primary; positions benchmark as
  complementary to SWE-bench. Agent must find bugs that pass-test-driven
  evaluation cannot detect. Predicted headline finding: SOTA agents with
  >60% SWE-bench score will achieve <20% on this benchmark.
- (B) **Adversarial reasoning on formal artifacts** — philosophical
  layer; reductions as miniature math claims, counterexample search as
  constructive falsification.
- (C) **Agentic QA / independent verification** — industrial relevance
  layer.

Operational implication noted: pilot scoring must distinguish "bug on
test-covered code path" (true test-escaping) from "bug on uncovered
path" (mere coverage gap). Suggested adding `cargo tarpaulin`-style
coverage tagging to pilot.

## Phase 1 — Exchange 9 (user) — arena framework architecture

User asked architecture + capability isolation. Mentor delivered an
over-engineered 7-component answer with multiple sub-tables. **User
pushed back: "什么玩意,我看不懂你在说什么".** Mentor reset, gave a
plain-language version, asked what part to redo.

## Phase 1 — Exchange 10 (user) — ground in real bugs first

User correctly redirects: **before designing, analyze what real bugs
look like.** Points to issue #1006 in CodingThrust/problem-reductions,
asks mentor to read body and comments.

Mentor fetched #1006: 16 bugs total (5 in body, 11 in comment), all
classified with root causes. Extracted the **bug taxonomy** that has
since become the design's anchor:

- **A. Soundness** (8 rules) — NO-instance maps to feasible target.
  Tests miss because they only exercise YES instances.
- **B. Extraction** (3 rules) — round-trip returns valid but
  non-optimal source solution.
- **C. Overhead formula** (4 rules) — reduction works, declared
  polynomial wrong.
- **D. Panic** (1 rule) — degenerate input crashes.

Two empirical observations from #1006 now driving design:
- Bug density ~23% (16 / ~70 non-ILP rules) — green light, library
  has more than enough latent bugs for an Arena to work.
- Test suite has structural blind spot (no NO-instance round-trip,
  no optimality check, no overhead empirical check) — these blind
  spots ARE the agent task.

Mentor's mutation-shaped framing was wrong. Real bugs are conceptual
design errors (necessary vs sufficient conditions, ≤ vs =, parity
assumptions, optimality preservation). Mutation operators cannot
generate them; the Arena scoring should measure agent ability to
catch THESE patterns, not synthetic mutations.

## Phase 1 — Exchange 11 (user) — live arena confirmed

User: **"我还是想做成 live arena 的形式"**. Decision locked.

Mentor proposes 4-oracle design (O_A / O_B / O_C / O_D) directly
mapped to #1006 families. Each agent submission = `(rule_id,
oracle_type, input)`. Score = 4-tuple `(N_A, N_B, N_C, N_D)` with
by-rule dedup. Headline capability metric = `(N_B + N_C) -
baseline_fuzzer` because A and D are mostly fuzzer-reachable.

Asked user about season length / library evolution rate.

## Phase 1 — Exchange 12 (user) — season cadence

User: **"长度不重要吧,就随着大版本更新更新就好了"**. Pragmatic call,
mentor agrees. Implication: paper 2 needs only Season 1 (snapshot at
submission time), no calendar-based season planning required.

---

## Locked-in design as of exchange 12

- **Format:** Live Arena (not Benchmark)
- **Capability framed as:** test-escaping bug discovery (primary);
  adversarial reasoning on formal artifacts (philosophical layer);
  agentic QA / independent verification (industrial layer)
- **Snapshot:** frozen at the SHA matching each `problem-reductions`
  major-version release
- **Oracle:** 4-pronged (O_A soundness / O_B extraction / O_C
  overhead / O_D panic), all mechanical pass/fail, no human/LLM judge
- **Submission unit:** `(rule_id, oracle_type, input)`
- **Dedup:** by `(rule, family)` — at most 1 point per cell
- **Score:** 4-tuple `(N_A, N_B, N_C, N_D)` reported separately;
  headline = `(N_B + N_C) - baseline_fuzzer`
- **Baselines on leaderboard:** random fuzzer + brute-force, proptest
  default, one-shot LLM, brute-force ceiling on small inputs, human
  expert (already implicit from #1006)
- **Compute:** per-agent budget, wall-clock + token cap (specifics TBD)
- **Scaffold policy:** any scaffold accepted; agent system as a whole
  is the unit measured (METR/RE-Bench tradition)
- **Solo first, adversarial as future work**

## Open / unresolved as of exchange 12

- Per-agent compute budget specifics (wall-clock hours, token cap)
- Submission flow / audit & replay infrastructure
- Whether to keep a "discovered bugs in supposedly-clean rules"
  bonus metric on top of the 4-oracle main score
- Pilot needed? — #1006 already gives strong signal that bugs exist,
  but pilot would still confirm agents can find them in current
  snapshot
- Whether to scan other closed issues (beyond #1006) for additional
  taxonomic data points

## Phase 2 — Exchange 13/14 (user) — pipeline + draft requested

User asked for a standard-arena evaluation pipeline first, then asked
for a draft in `../ProblemReductionBenchmarkPaper/`. Mentor delivered
both:

- **Pipeline:** 6 stages (Pre-Season Setup, Agent Registration,
  Per-Cell Execution, Verification, Scoring, Audit & Publish) modeled
  on SWE-bench / Cybench. Per-cell containerized execution, oracle
  CLI subcommand (verify / submit), wall-clock + token budget,
  by-rule dedup. Cost estimate: Season-1-lite (50 rules × 4 oracles
  × 5 agents) ≈ $10K-25K total; pilot (20 × 4 × 3) fits a weekend
  at $1K-12K.

- **Draft (NeurIPS 2026 E&D track):** wrote `main.tex` with the
  following sections:
  1. Abstract — frames test-escaping bug discovery and ReductionArena
  2. Introduction — gap left by SWE-bench-style benchmarks; oracle
     problem solved by reduction round-trip; four contributions
  3. Background — reductions as a substrate
  4. A Taxonomy of Test-Escaping Defects — 4 families from #1006
     with concrete rule examples; explicit "why each escapes the
     test suite" paragraph
  5. The Arena — live snapshot model, submission unit, oracles,
     scoring with `(N_B + N_C) - baseline` headline, dedup, season
     cadence
  6. Pipeline and Implementation — containerized per-cell execution,
     verification/audit, cost
  7. Pilot Results — placeholder TODO with structured plan
  8. Related Work — code benchmarks, agentic arenas, mutation/PBT,
     companion paper reference
  9. Limitations — domain narrowness, bug-supply consumption,
     contamination, solver dependence, scaffold heterogeneity
  10. Discussion — co-verification flywheel, adversarial mode,
      generalization
  11. Conclusion

  Also added bib entries for SWE-bench, HumanEval, LiveCodeBench,
  Chatbot Arena, MLE-bench, Cybench, Jia & Harman 2011, QuickCheck,
  and a placeholder for the companion problem-reductions paper.
  Several entries marked `note = TODO: verify ...` because exact
  bibliographic details were uncertain.

  Compiled cleanly to a 14-page PDF.

## Phase 3 wrap-up TODO when session ends

Outstanding items if user wants to continue:
- Verify all bib entries (some marked TODO)
- Fill pilot results once experiments run
- Consider scanning more closed issues to expand taxonomy
- Decide author list and switch to non-anonymous if desired

